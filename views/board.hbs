<h1 class="pt-3 mb-0" style="text-align: center;">🛸/UFO</h1>

<div style="text-align: center;">
🖊️ <a href="#" class="select" style="font-size: 16pt;">Start a Thread</a>
</div>

<hr width="85%">

<div style="text-align: center;">
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}

	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}
	{{> post}}

	{{> post}}
	{{> post}}
</div>